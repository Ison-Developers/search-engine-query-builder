define([], function () {
  var schema = {
    __placeholder: '(-.-)',
    query: {
      types: {
        and: 'q=AND((-.-))',
        or: 'q=OR((-.-))',
        regular: 'q=(-.-)'
      }
    },
    pageSize: 'rows=(-.-)',
    startPage: 'start=(-.-)'
  };

  return schema;
});

